# PREF-002：CPU（XSynth）离线导出提速记录

> 记录日期：2026-09-23 ｜ 关联 Issue：#43 ｜ 分支：`perf/pref-002-cpu-export-speed`
> 素材：`Night Voyager-18906416-By IMT11D&TheAtomicBomb.mid`（153 MB / 130 轨 / 1920 PPQN / 3827 万事件 / 334.87s）
> 音源：`F:\CathanConcertGrand-1.1.1\1. Cathan Concert Grand - Concert Presets\Cathan Concert Grand.sfz`

## 一、结论速览

| 场景 | 结果 |
|---|---|
| Issue 原始测量（**debug**，逐事件渲染，channel/key 均 Auto） | **4 h 55 m**（0.019× 实时） |
| 本轮修复后（release，**应用默认口径**：每键 32 + 限幅开 + Auto+Auto + B=256） | **165 s（应用实测）/ 239 s（无头复核）**（1.94×/1.33× 实时） |
| Issue 验收初目标（release ≤30 min） | ✅ **达成**（余量 3.6×~10.9×） |
| 修复后（release，每键复音 = 0 无限制口径，无头实测） | 59.3 min（0.090×） |
| 分片对照（250-260s，同配置 key=Auto + 复音无限 + 无限幅） | 20.59s → 13.73s（**1.50×**） |
| 分片对照（250-260s，key 级并行关闭） | 78.03s → 36.13s（2.16×） |

**核心机制**：CPU 导出原为"逐事件驱动"——每个 MIDI 事件都调用一次合成引擎（16 通道 × 128 键
簿记、声部维护、并行池调度）。本修复把事件按 B 帧块聚合、块首一次性投递后整块渲染，调用次数
从"事件数（百万级）"降到"音频帧数 / B（万级）"；同时 t ick→时间换算改为 O(1) 摊还游标。

**关键互补发现**：块式调度让 **key 级并行真正有活可干**——原逐事件模式下每块只有 0~5 帧，
并行调度开销大于实际 DSP；块化后同一配置下 key=Auto 比 key=None 快 **2.6×**（分片），
全曲上快 **4.2×**（59.3 min vs key=None 投影 4.2 h）。

## 二、实现（两个 PR）

### PR #61（已合并，merge `acd3ec6c`）

1. **P0-2 `TickToTime` 游标化**（`922c06f5`）
   - `advance_to` 原每次调用两次全段线性扫描（O(tempo 段数)），改为段索引 + 累计秒游标，O(1) 摊还；
   - 顺带以整数帧域累积替代逐事件 `(delta×sr) as usize` 截断，修掉亚采样时基漂移
     （该素材 1.1% 的渲染时长误差：修复前少渲染 5280 帧/12s 片段）。

2. **P0-1 CPU 块式渲染调度**（`fedcd5a5`）
   - 新增 `crates/tools/export/src/audio/block_scheduler.rs`（`RenderCursor`：块对齐 / 精确模式 / 收尾补齐）；
   - `event/processor.rs` 拆出 `event/render.rs`（帧域批量渲染 + 缓冲池），
     `dispatch_event` / `frame_at_tick` 取代逐事件驱动；
   - 配置项 `block_frames`（默认 **256**，`0/1` = 逐事件精确模式）；
   - `note_force_end_delay > 0` 强制精确模式（语义保真）；暂停/中止按块检查；
     收尾补齐到"最后一个事件的精确帧"，输出长度与旧实现一致。

### PR #62（本 PR）

3. **P1-4 多轨归并**（`1c0c236e`）：`StreamingMidiPlayer::next_event` 原每事件线性扫描全部轨道
   （O(轨道数)/事件，130 轨 × 3827 万事件 ≈ 50 亿次比较），改为每轨游标 + 最小堆 k 路归并；
   同 tick 事件保持"轨号升序"稳定语义（新增回归测试）。
4. **dev profile**：`[profile.dev.package.lumino-export]` / `lumino-midi-loader` 加 `opt-level=3`，
   避免 debug 构建下导出不可用（原 debug 差距的另一来源）。

## 三、基准（release，48 kHz 立体声）

### 分片（250-260s，130 轨 / 34 万事件 / 10s 乐谱，12s 输出含尾）

| 配置 | 墙钟 | ×实时 |
|---|---|---|
| 旧逐事件（key=None） | 78.03s | 0.152 |
| 旧逐事件（**Auto+Auto，应用默认**） | 20.59s | 0.578 |
| 新 B=1 精确（Auto+Auto） | 20.85s | 0.575 |
| 新 B=64（Auto+Auto） | 16.56s | 0.725 |
| 新 B=128（Auto+Auto） | 14.97s | 0.802 |
| **新 B=256（默认，Auto+Auto）** | **13.96s** | **0.860** |
| 新 B=512（Auto+Auto） | 13.59s | 0.883 |

### 全曲（334.87s 素材）

| 配置 | 墙钟 | ×实时 | 备注 |
|---|---|---|---|
| **修复后 B=256，应用默认（每键 32 + 限幅开 + Auto+Auto）** | **165 s（应用）/ 239 s（无头）** | 1.94× / 1.33× | 输出 116.95 MB / 1532.9 万帧 |
| 修复后 B=256（每键 0 无限 + 无限幅 + Auto+Auto） | 59.3 min（3559.9s） | 0.090 | 口径对照（59.3 min 即此配置） |
| 修复后 B=256（key=None） | 中断于 32.1s 音频 / 24.1 min | 0.022 | 投影 ≈4.2 h，故中断 |

## 四、正确性证据

1. **精确模式（B=1）与被替换实现对照**（真实小素材 150-151s，119 事件）：
   最佳对齐仅 **3 帧**（≈0.06 ms，漂移修复）、对齐后 corr **0.989**、1 ms RMS 包络 corr **0.996**。
2. **块模式的差异是"每音符 ≤B 帧（256 帧 @48kHz = 5.33 ms）的确定性量化"**（对齐 GPU 后端
   512 帧块语义）；逐样本相关系数对亚毫秒平移极敏感，不构成质量判据——A/B 结论以包络与听感为准。
3. 引擎确定性验证：同代码两次渲染 corr = 1.000、自比较 corr = 1.000。
4. 单测：`TickToTime` 游标与慢路径逐点数值等价（跨段/重复/回退 tick）、`RenderCursor` 5 例、
   `render_frames` 帧数守恒、同 tick 轨号顺序回归。
5. 进度/暂停/中止/尾部语义：按块检查（≤4096 帧粒度），`finalize` 逻辑未改。

## 五、复音上限（layers）与耗时：验收口径说明

耗时对"每键复音上限"高度敏感（250-260s 分片，B=256 / Auto+Auto / 限幅开）：

| layers | 墙钟 | 相对 32 |
|---|---|---|
| **32（应用默认，命中验收）** | **3.42s** | — |
| 64 | 4.74s | +39% |
| 128 | 7.04s | +106% |
| 256 | 23.7s | +592% |
| 0（无限） | ≈14s（分片）/ 59.3 min（全曲） | 全曲超线性劣化 |

原因：本素材是 **7 端口文件**（FF 21 ×129，端口 0–6），当前 16 通道引擎把端口合流，
"每键 32"实际被多个端口共享；设为 0 则声部无上限堆积（内存与耗时暴涨）。

后续（可选，非验收必需）：
- **多端口渲染支持**（正确性议题，另开单）：引擎 `port*16+ch` 映射后，同值会保留更多层、音色不再串台；
- fork 侧 P1-3（`remove_ended_voices` 去分配）/ P1-5（键级负载均衡）/ P2（流水线）——如需进一步压缩耗时再排期。

## 六、复现方式（临时工具不入库）

```text
# 切片器（temp：midi-slice，Rust + midly）
midi-slice <in.mid> <out.mid> <start_s> <end_s>

# 基准器（crates/tools/export/examples/cpu_export_bench.rs，examples/ 被 .gitignore 忽略）
cargo run --release -p lumino-export --example cpu_export_bench -- \
  --midi <切片.mid> --sfz <音源.sfz> --out <输出.wav> \
  [--block 256] [--channel-threads auto] [--key-threads auto] [--compare <参考.wav>]
# 输出：BENCH wall_s / audio_s / realtime_x / output_mb [+ compare/aligned/envelope 指标]
```

## 七、验收对照（Issue #43）

| # | 标准 | 结果 |
|---|---|---|
| 1 | release 全曲 ≤30 min | ✅ **165 s（应用默认口径实测）/ 239 s（无头复核）** |
| 2 | 输出质量不回退 | ✅ 精确模式实质等价（§四）；块模式为 ≤5.33 ms 确定性量化，B 可配（`block_frames`） |
| 3 | 进度/暂停/中止/尾渲染语义保持 | ✅ 按块检查 + 收尾补齐，`finalize` 未改 |
| 4 | fmt / clippy / 新基线记录 | ✅ CI 全绿；本文档即基线记录 |
