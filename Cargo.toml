[workspace]
members = ["crates/core", "crates/gfx", "crates/midi", "crates/ui"]
resolver = "3"

[workspace.package]
version = "0.1.0"
rust-version = "1.92.0"
homepage = "https://github.com/BuickMeow/lumino-rust"
repository = "https://github.com/BuickMeow/lumino-rust"
license = "Mulan PSL v2"
edition = "2024"

[package]
name = "lumino-rs"
version.workspace = true
rust-version.workspace = true
homepage.workspace = true
license.workspace = true
edition.workspace = true

[workspace.dependencies]
lumino-core = { path = "crates/core" }
lumino-gfx = { path = "crates/gfx" }
lumino-midi = { path = "crates/midi" }
midly = { git = "https://github.com/Enderman-bm/midly-fork", rev = "c21f746f7ab4d92538527ddf7f08904650cbc80e" }
lumino-ui = { path = "crates/ui" }
# crates.io
iced_renderer = { version = "0.14.0", features = ["wgpu", "tiny-skia"] }
tracing = "0.1.43"
tracing-subscriber = { version = "0.3.22", features = ["env-filter"]}
thiserror = "2.0.17"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"

[dependencies]
lumino-core = { workspace = true }
lumino-gfx = { workspace = true }
lumino-midi = { workspace = true }
lumino-ui = { workspace = true }
# crates.io
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
# Private dependencies
winit = "0.30"
futures = "0.3.31"
directories = "6.0.0"
toml = "0.9.11"
rfd = "0.15"
midly = { workspace = true }
tokio = { version = "1.42", features = ["full"] }

[target.'cfg(target_os = "macos")'.dependencies]
muda = "0.17.1"

[profile.dev]
incremental = true # Compile your binary in smaller steps.

[profile.release]
codegen-units = 1 # Allows LLVM to perform better optimization.
lto = true # Enables link-time-optimizations.
opt-level = "s" # Prioritizes small binary size. Use `3` if you prefer speed.
panic = "abort" # Higher performance by disabling panic handlers.
strip = true # Ensures debug symbols are removed.
